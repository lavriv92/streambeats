define(['jquery', 'underscore', 'backbone', 
    '../collections/artists'], function($, _, Backbone, Artists) {
  var ArtistsView = Backbone.View.extend({
    render: function() {
      
    }
  });

  return ArtistsView;
});
